name: Artifact

on: [push]

env:
  ARTIFACT_NAME: myartifact

jobs:
  main:
   runs-on: ubuntu-latest
   steps: 
   - name: checkout the code
     uses: actions/checkoutt@v2
   - name: Upload the artifact
     uses: actions/upload-artfactt@v2
     with:
       name: ${{ env.ARTIFACT_NAME }}
       path: .
